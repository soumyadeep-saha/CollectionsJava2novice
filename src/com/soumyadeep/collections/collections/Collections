The Collections class consists of static methods that exclusively operates on collections and returns collections. It contains polymorphic algorithms that operate on collections, "wrappers", which return a new collection backed by a specified collection, and a few other odds and ends. The methods of Collections class all throw a NullPointerException if the collections or class objects provided to them are null.